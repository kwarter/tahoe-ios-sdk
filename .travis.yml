
language: objective-c

before_install:
    - gem update --system
    - gem install ios_ci
    - gem install cocoapods
    - brew install ios-sim

xcode_project: TahoeSDK.xcodeproj

xcode_scheme: TahoeSDKTests
